# F0 native runtime evidence

This directory retains compact configuration, environment, coverage, metrics,
and manifest evidence for the current-source F0 native shadow. The exact
cross-offload result is authoritative only through the corresponding
`oracles/` receipt. Runtime metrics support a bounded eight-step throughput
comparison, not a general benchmark.
